## Location Commands — Practice Log

**What I did here:**
I started by checking where I was with `pwd`, which showed I was in the
root folder. I used `ls` to see what was already there. Then I created
a new folder called `cybersecurity-practice` with `mkdir`, and moved
into it with `cd`.

Once inside, I ran `pwd` again to confirm I had actually moved into
the new folder, not just created it. I also tried `ls -a` to see
hidden files (there were none yet, since the folder was empty). I made
a small typo the first time (`ls-a` instead of `ls -a`), which gave a
"command not found" error. That's expected, since Linux commands need
a space before their flags. I fixed it right after.

Finally, I used `cd ..` to go back one folder, and `cd ~` to jump
straight back to my home folder, confirming both with `pwd`.

